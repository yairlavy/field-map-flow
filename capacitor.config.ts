import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yairlavy.fieldmapflow',
  appName: 'field-map-flow',
  webDir: 'dist'
};

export default config;
